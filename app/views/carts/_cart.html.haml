%table.table
  %thead
    %tr
      %th 商品名
      %th 価格(税抜)
      %th 数量
      %th 削除
  %tbody
    = render cart.line_items.includes(:product)
  %tfoot
    %tr
      %th{ colspan: 2 } 合計金額
      %td= number_to_currency(cart.total_price)
