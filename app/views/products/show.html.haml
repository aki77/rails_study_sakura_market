%h1= @product.name

%p= @product.description

%dl
  %dt 価格
  %dd= number_to_currency(@product.price)

= form_for LineItem.new(product: @product), url: product_line_item_path(@product) do |f|
  = f.select :quantity, options_for_select(1..30)
  = f.submit 'カートに追加'
